# 1. Dependencies (Never commit these, they are huge)
node_modules/

# 2. Build Output (Ignore the compiled JS, we only want the TS source)
dist/
build/

# 3. Security (CRITICAL: Never commit your API keys!)
.env
.env.local
.env.*

# 4. Logs and System Files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs/
*.log

# 5. OS Junk Files
.DS_Store
Thumbs.db

# 6. Editor Settings (Optional: Keeps your personal settings off GitHub)
.vscode/
.idea/